<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztzhome.zblog.mapper.ArticleMapper">

    <resultMap id="ArticleResultMap" type="cc.ztzhome.zblog.bean.entity.Article">
        <id property="articleId" column="article_id" />
        <result property="userId" column="user_id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="coverKey" column="cover_key" />
        <result property="articleType" column="article_type" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <insert id="insertArticle" parameterType="cc.ztzhome.zblog.bean.entity.Article"
            useGeneratedKeys="true" keyProperty="articleId">
        INSERT INTO tb_article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            user_id,
            title,
            content,
            article_type,
            status,
            create_time,
            <if test="coverKey != null and coverKey != ''">
                cover_key,
            </if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{userId},
            #{title},
            #{content},
            #{articleType},
            #{status},
            #{createTime},
            <if test="coverKey != null and coverKey != ''">
                #{coverKey},
            </if>
        </trim>
    </insert>

    <select id="selectById" resultMap="ArticleResultMap">
        SELECT * FROM tb_article WHERE article_id = #{articleId}
    </select>

    <select id="selectPublishedList" resultMap="ArticleResultMap">
        SELECT * FROM tb_article WHERE status = 1 ORDER BY create_time DESC
    </select>

    <select id="selectRandomPublishedList" resultMap="ArticleResultMap">
        SELECT * FROM tb_article WHERE status = 1 ORDER BY RAND() LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByUserId" resultMap="ArticleResultMap">
        SELECT * FROM tb_article WHERE user_id = #{userId} AND status = 1 ORDER BY create_time DESC
    </select>

    <update id="updateCoverKey">
        UPDATE tb_article SET cover_key = #{coverKey} WHERE article_id = #{articleId}
    </update>

    <update id="updateStatus">
        UPDATE tb_article SET status = #{status} WHERE article_id = #{articleId}
    </update>

</mapper>
