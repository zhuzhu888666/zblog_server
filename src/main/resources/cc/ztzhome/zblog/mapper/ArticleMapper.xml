<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztzhome.zblog.mapper.ArticleMapper">

    <resultMap id="ArticleResultMap" type="cc.ztzhome.zblog.bean.entity.Article">
        <id property="articleId" column="article_id" />
        <result property="userId" column="user_id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="coverKey" column="cover_key" />
        <result property="articleType" column="article_type" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="likeCount" column="like_count" />
    </resultMap>

    <insert id="insertArticle" parameterType="cc.ztzhome.zblog.bean.entity.Article"
            useGeneratedKeys="true" keyProperty="articleId">
        INSERT INTO tb_article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            user_id,
            title,
            content,
            article_type,
            status,
            create_time,
            <if test="coverKey != null and coverKey != ''">
                cover_key,
            </if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{userId},
            #{title},
            #{content},
            #{articleType},
            #{status},
            #{createTime},
            <if test="coverKey != null and coverKey != ''">
                #{coverKey},
            </if>
        </trim>
    </insert>

    <select id="selectById" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a WHERE a.article_id = #{articleId}
    </select>

    <select id="selectPublishedList" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a WHERE a.status = 1 ORDER BY a.create_time DESC
    </select>

    <select id="selectRandomPublishedList" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a WHERE a.status = 1 ORDER BY RAND() LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByUserId" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a WHERE a.user_id = #{userId} AND a.status = 1 ORDER BY a.create_time DESC
    </select>

    <update id="updateCoverKey">
        UPDATE tb_article SET cover_key = #{coverKey} WHERE article_id = #{articleId}
    </update>

    <update id="updateStatus">
        UPDATE tb_article SET status = #{status} WHERE article_id = #{articleId}
    </update>

    <select id="selectByPageWithFilter" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a
        <if test="tagId != null">
            INNER JOIN tb_article_tag at ON a.article_id = at.article_id
        </if>
        <where>
            <if test="keyword != null and keyword != ''">
                AND a.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null">
                AND a.status = #{status}
            </if>
            <if test="tagId != null">
                AND at.tag_id = #{tagId}
            </if>
        </where>
        ORDER BY a.create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countWithFilter" resultType="long">
        SELECT COUNT(*) FROM tb_article a
        <if test="tagId != null">
            INNER JOIN tb_article_tag at ON a.article_id = at.article_id
        </if>
        <where>
            <if test="keyword != null and keyword != ''">
                AND a.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null">
                AND a.status = #{status}
            </if>
            <if test="tagId != null">
                AND at.tag_id = #{tagId}
            </if>
        </where>
    </select>

    <select id="selectByTagId" resultMap="ArticleResultMap">
        SELECT a.*,
               (SELECT COUNT(*) FROM tb_blog_like WHERE article_id = a.article_id) AS like_count
        FROM tb_article a
        INNER JOIN tb_article_tag at ON a.article_id = at.article_id
        WHERE at.tag_id = #{tagId} AND a.status = 1
        ORDER BY a.create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTagId" resultType="long">
        SELECT COUNT(*)
        FROM tb_article a
        INNER JOIN tb_article_tag at ON a.article_id = at.article_id
        WHERE at.tag_id = #{tagId} AND a.status = 1
    </select>

    <update id="updateArticle">
        UPDATE tb_article
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="articleType != null">article_type = #{articleType},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE article_id = #{articleId}
    </update>

</mapper>
