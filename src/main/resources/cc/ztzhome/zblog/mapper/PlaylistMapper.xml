<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztzhome.zblog.mapper.PlaylistMapper">

    <resultMap id="PlaylistResultMap" type="cc.ztzhome.zblog.bean.entity.Playlist">
        <id property="playlistId" column="playlist_id" />
        <result property="userId" column="user_id" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="coverPath" column="cover_path" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="deleted" column="deleted" />
    </resultMap>

    <insert id="insert" parameterType="cc.ztzhome.zblog.bean.entity.Playlist"
            useGeneratedKeys="true" keyProperty="playlistId">
        INSERT INTO tb_playlist (user_id, name, description, cover_path, create_time, update_time)
        VALUES (#{userId}, #{name}, #{description}, #{coverPath}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectById" resultMap="PlaylistResultMap">
        SELECT * FROM tb_playlist WHERE playlist_id = #{playlistId} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="PlaylistResultMap">
        SELECT * FROM tb_playlist WHERE user_id = #{userId} AND deleted = 0 ORDER BY create_time DESC
    </select>

    <update id="update" parameterType="cc.ztzhome.zblog.bean.entity.Playlist">
        UPDATE tb_playlist SET update_time = NOW()
        <if test="name != null">, name = #{name}</if>
        <if test="description != null">, description = #{description}</if>
        <if test="coverPath != null">, cover_path = #{coverPath}</if>
        WHERE playlist_id = #{playlistId}
    </update>

    <update id="deleteById">
        UPDATE tb_playlist SET deleted = 1, update_time = NOW() WHERE playlist_id = #{playlistId}
    </update>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM tb_playlist WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>
