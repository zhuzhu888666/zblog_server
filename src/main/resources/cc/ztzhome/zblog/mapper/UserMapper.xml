<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztzhome.zblog.mapper.UserMapper">

    <!-- 定义 User 实体类与数据库表 user 的映射 -->
    <resultMap id="UserResultMap" type="cc.ztzhome.zblog.bean.entity.User">
        <id property="userId" column="user_id" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="role" column="role" />
        <result property="nickname" column="nickname" />
        <result property="gender" column="gender" />
        <result property="introduction" column="introduction" />
        <result property="status" column="status" />
        <result property="userAvatar" column="user_avatar" />
        <result property="createTime" column="create_time" />
        <result property="birthday" column="birthday" />
    </resultMap>

    <!-- 动态插入 SQL：主键自增，使用 useGeneratedKeys 返回生成的主键 -->
    <insert id="insertUser" parameterType="cc.ztzhome.zblog.bean.entity.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO tb_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            email,
            password,
            role,
            status,
            create_time,
            <if test="nickname != null and nickname != ''">
                nickname,
            </if>
            <if test="gender != null">
                gender,
            </if>
            <if test="introduction != null and introduction != ''">
                introduction,
            </if>
            <if test="userAvatar != null and userAvatar != ''">
                user_avatar,
            </if>
            <if test="birthday != null">
                birthday,
            </if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{email},
            #{password},
            #{role},
            #{status},
            #{createTime},
            <if test="nickname != null and nickname != ''">
                #{nickname},
            </if>
            <if test="gender != null">
                #{gender},
            </if>
            <if test="introduction != null and introduction != ''">
                #{introduction},
            </if>
            <if test="userAvatar != null and userAvatar != ''">
                #{userAvatar},
            </if>
            <if test="birthday != null">
                #{birthday},
            </if>
        </trim>
    </insert>

    <!--根据user_id查询-->
    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM tb_user WHERE user_id = #{userId}
    </select>
    <select id="selectAllByEmail" resultType="cc.ztzhome.zblog.bean.entity.User">
        SELECT * FROM tb_user WHERE email=#{email}
    </select>
    <!-- 根据邮箱查询用户是否存在（返回 true/false） -->
    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM tb_user WHERE email = #{email}
        )
    </select>

    <!-- 更新用户密码 -->
    <update id="updatePassword">
        UPDATE tb_user SET password = #{password} WHERE user_id = #{userId}
    </update>

    <!-- 更新用户资料 -->
    <update id="updateProfile">
        UPDATE tb_user
        <set>
            <if test="nickname != null and nickname != ''">nickname = #{nickname},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="introduction != null">introduction = #{introduction},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户头像 -->
    <update id="updateAvatar">
        UPDATE tb_user SET user_avatar = #{userAvatar} WHERE user_id = #{userId}
    </update>
</mapper>