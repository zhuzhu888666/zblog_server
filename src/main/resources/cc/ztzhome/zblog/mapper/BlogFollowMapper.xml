<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztzhome.zblog.mapper.BlogFollowMapper">

    <resultMap id="BlogFollowResultMap" type="cc.ztzhome.zblog.bean.entity.BlogFollow">
        <id property="followId" column="follow_id" />
        <result property="followerId" column="follower_id" />
        <result property="followeeId" column="followee_id" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <insert id="insert" parameterType="cc.ztzhome.zblog.bean.entity.BlogFollow"
            useGeneratedKeys="true" keyProperty="followId">
        INSERT INTO tb_user_follow (follower_id, followee_id, create_time)
        VALUES (#{followerId}, #{followeeId}, #{createTime})
    </insert>

    <delete id="deleteByFollowerAndFollowee">
        DELETE FROM tb_user_follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <select id="selectByFollowerAndFollowee" resultMap="BlogFollowResultMap">
        SELECT * FROM tb_user_follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </select>

    <select id="selectFollowersByUserId" resultMap="BlogFollowResultMap">
        SELECT * FROM tb_user_follow WHERE followee_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectFollowingByUserId" resultMap="BlogFollowResultMap">
        SELECT * FROM tb_user_follow WHERE follower_id = #{userId} ORDER BY create_time DESC
    </select>

</mapper>
